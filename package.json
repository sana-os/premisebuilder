{
  "name": "premise-builder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/core.js && node --check src/storage.js && node --check src/validation.js && node --check src/merge.js && node --check src/import-export.js && npm test && npm run build && node scripts/check-static.mjs"
  }
}
