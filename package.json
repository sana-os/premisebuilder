{
  "name": "premise-builder",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "localize": "node scripts/generate-ja-questions.mjs && node scripts/generate-es-questions.mjs",
    "localize:ja": "node scripts/generate-ja-questions.mjs",
    "localize:es": "node scripts/generate-es-questions.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "npm run localize && node --check src/app.js && node --check src/core.js && node --check src/storage.js && node --check src/validation.js && node --check src/merge.js && node --check src/import-export.js && npm test && npm run build && node scripts/check-static.mjs"
  }
}
